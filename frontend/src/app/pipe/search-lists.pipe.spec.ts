import { SearchListsPipe } from './search-lists.pipe';

describe('SearchListsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchListsPipe();
    expect(pipe).toBeTruthy();
  });
});
